# Liquidity Risk Management — Python dependencies
#
# Setup (Windows / macOS / Linux):
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)   or   source .venv/bin/activate   (macOS/Linux)
#   pip install -r requirements.txt
#   streamlit run src/app.py
#
# Requires Python 3.10+ (developed and verified on 3.12).

# ── Core app ─────────────────────────────────────────────────────────────────
streamlit==1.55.0
pandas==2.3.3
numpy==2.4.2
PyYAML==6.0.3
altair==6.0.0

# ── Excel read/write ─────────────────────────────────────────────────────────
# openpyxl is required — it writes the OJK export workbooks and is the
# fallback reader if python-calamine is unavailable on a platform.
openpyxl==3.1.5
# python-calamine is optional but strongly recommended: a Rust-based Excel
# reader that safe_read_excel() (src/lcr_engine.py) prefers automatically when
# installed — roughly 5-6x faster than openpyxl on this project's source
# files, which is most of the "Run Analysis" wait time on the LCR/NSFR/Stress
# Testing pages. The app runs correctly without it (falls back to openpyxl).
python-calamine==0.8.2

# ── AI Executive Summary page (optional feature) ────────────────────────────
# Only needed for the "AI Executive Summary" page. The rest of the app
# (LCR, NSFR, Stress Testing, Audit Log) works without these two.
anthropic==0.116.0
python-dotenv==1.2.2
