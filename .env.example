# Liquidity Risk Management — environment configuration template.
# Copy this file to `.env` and fill in real values. `.env` itself is
# git-ignored and must never be committed.

# ── AI Executive Summary (OpenModel gateway) ────────────────────────
# Required. Create a key at https://openmodel.ai (looks like om-a1f4-...).
OPENMODEL_API_KEY=om-your-real-key

# Optional — defaults shown.
# OPENMODEL_MODEL=deepseek-v4.1-flash
# OPENMODEL_BASE_URL=https://api.openmodel.ai
